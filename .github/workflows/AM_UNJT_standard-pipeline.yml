name: UNJT Pipeline

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '.github/workflows/**'
      - 'renovate.json'
      - 'README.md'
    branches:
      - 'main'
      - 'develop'
      - 'feat/**'
jobs:
  job2:
    runs-on: ubuntu-latest
    steps:
      - run: echo test

  job1:
    uses: turbo5000c/am-reuseable-workflow/.github/workflows/main-workflow.yml@main
