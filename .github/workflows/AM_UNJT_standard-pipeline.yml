name: Z4UJ Pipeline

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '.github/workflows/**'
      - 'renovate.json'
      - 'README.md'
    branches:
      - 'main'
      - 'develop'
      - 'feat/**'
  
jobs:
  build:
    uses: turbo5000c/am-reuseable-workflow/.github/workflows/AM_Z4UJ_main-workflow.yml@main
    secrets: inherit