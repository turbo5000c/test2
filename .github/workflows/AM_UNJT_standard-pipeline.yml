name: UNJT Pipeline

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '.github/workflows/**'
      - 'renovate.json'
      - 'README.md'
    branches:
      - 'main'
      - 'develop'
      - 'feat/**'
jobs:
  job1:
    uses: turbo5000c/am-reuseable-workflow/.github/workflows/AM_123_main-workflow.yml@main
    secrets: inherit
