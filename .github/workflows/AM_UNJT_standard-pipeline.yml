name: UNJT Pipeline 2

on:
  workflow_dispatch:
  push:
    paths-ignore:
      - '.github/workflows/**'
      - 'renovate.json'
      - 'README.md'
    branches:
      - 'main'
      - 'develop'
      - 'feat/**'

permissions:
  contents: write
  actions: read
  
jobs:
  build:
    uses: turbo5000c/am-reusable-workflow/.github/workflows/AM_123_main-workflow.yml@main
    secrets: inherit
